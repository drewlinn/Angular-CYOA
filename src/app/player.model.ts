export class Player { constructor (public name: string, public gender: string, public body: number, public heart: number, public mind: number, public spirit: number, public inventory: string[], public gold: number = 0) { }
}
